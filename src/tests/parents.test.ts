import { _SERVICE } from "../declarations/parent.declarations";
import { parentTest } from "../functions/parent.function";

describe("Test if parent canisters are setup correctly", () => {
  parentTest();
});
